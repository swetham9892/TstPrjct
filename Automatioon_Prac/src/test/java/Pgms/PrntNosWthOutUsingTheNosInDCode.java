package Pgms;

public class PrntNosWthOutUsingTheNosInDCode {
public static void main(String[] args) {
	//prnt 1 to 100 without nos
	int one = 'A'/'A';//a/a=1
	String sone = "..........";//length is 10
  for (int i = one; i <= (sone.length())*(sone.length()); i++) {
	System.out.println(i);
}
  //with ascky values a=97 b=98 c=99 d=100
  for (int i = one; i <= 'd'; i+=one) {
	System.out.println(i);
}

String s = "programmin"; //length is 10
		int zero = s.length()-s.length();
		int hundred = s.length() * s.length();
		for(int i = zero; i<=hundred; i++) {
			System.out.println(i);
}
		String a = "Bone";
		String b = "Aone";
		int i = a.compareTo(b);
		int j = (i+i+i)*(i+i+i)+i;
		for(int n=i;n<j*j;++n){
		    System.out.println(n);
		}
}
}
